#include <iostream>

using namespace std;

void compare(double a)
{
    if(a == round(a))
    {
        int b = a;
        int num1 = b / 10000;
        int num2 = a/
        int output;
        cout<<output;
    }
    else
    {
        double output = sqrt(a);
        cout<<output;
    }
}

int main()
{
    double input_num;
    cin>>input_num;
    compare(input_num);
}
