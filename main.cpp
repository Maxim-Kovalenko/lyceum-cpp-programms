#include <bits/stdc++.h>

using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}

string compare(a, b, c)
{
    int num1, num2, num3;
    if(a>b)
    {
        num1 = max(a, c)
        num2 = max(num1, b)
    }
    else
    {
        num1 = max(b, c)
        num2 = max(num1, a)
    }
}
